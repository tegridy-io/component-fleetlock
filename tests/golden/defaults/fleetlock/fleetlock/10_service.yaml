apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    name: fleetlock
  name: fleetlock
spec:
  clusterIP: 10.43.0.11
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    name: fleetlock
  sessionAffinity: None
  type: ClusterIP
